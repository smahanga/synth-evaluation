# SynthEvaluation — System Architecture

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              USER'S BROWSER                                 │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                     React Frontend (App.jsx)                          │  │
│  │                                                                       │  │
│  │  ┌─────────────┐   ┌──────────────┐   ┌─────────────────────────┐   │  │
│  │  │  LAYER 1    │   │   LAYER 2    │   │       LAYER 3           │   │  │
│  │  │  Persona    │   │   Target Bot │   │   Evaluation Pipeline   │   │  │
│  │  │  Engine     │   │   Library    │   │                         │   │  │
│  │  │             │   │              │   │  Grades bot on:         │   │  │
│  │  │  6 Personas │   │  5 Built-in  │   │  • Clarity              │   │  │
│  │  │  👵😡⚡💼🌍🕵️ │   │  ✏️ Custom   │   │  • Helpfulness          │   │  │
│  │  │             │   │  🔗 External │   │  • Tone & Empathy       │   │  │
│  │  │             │   │     API      │   │  • Safety               │   │  │
│  │  │             │   │              │   │  • Adaptability          │   │  │
│  │  └──────┬──────┘   └──────┬───────┘   └────────────┬────────────┘   │  │
│  │         │                 │                         │                │  │
│  │         └────────┬────────┘                         │                │  │
│  │                  ▼                                  │                │  │
│  │     ┌────────────────────────┐                      │                │  │
│  │     │   Simulation Engine    │──────────────────────┘                │  │
│  │     │   (runSimulation)      │                                      │  │
│  │     │                        │                                      │  │
│  │     │  For each turn (2-8):  │                                      │  │
│  │     │  1. Persona speaks     │                                      │  │
│  │     │  2. Bot responds       │                                      │  │
│  │     │  3. 3s cooldown        │                                      │  │
│  │     │  Then: AI evaluates    │                                      │  │
│  │     └───────────┬────────────┘                                      │  │
│  │                 │                                                    │  │
│  └─────────────────┼────────────────────────────────────────────────────┘  │
│                    │ fetch("/api/chat")                                     │
└────────────────────┼───────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     VERCEL SERVERLESS FUNCTIONS                              │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        /api/chat.js                                    │  │
│  │                                                                       │  │
│  │   ┌─────────────────────┐       ┌──────────────────────────────┐     │  │
│  │   │   Gemini API Path   │       │   External Bot Proxy Path    │     │  │
│  │   │                     │       │                              │     │  │
│  │   │  • Persona gen      │       │  • Receives externalUrl      │     │  │
│  │   │  • Bot responses    │       │  • Adds Basic Auth header    │     │  │
│  │   │  • Evaluation       │       │  • Forwards POST request     │     │  │
│  │   │  • System prompt    │       │  • Parses response formats:  │     │  │
│  │   │    injection        │       │    - Gemini format           │     │  │
│  │   │                     │       │    - OpenAI format           │     │  │
│  │   └──────────┬──────────┘       │    - Generic JSON            │     │  │
│  │              │                  └──────────────┬───────────────┘     │  │
│  └──────────────┼────────────────────────────────┼─────────────────────┘  │
│                 │                                 │                        │
└─────────────────┼─────────────────────────────────┼───────────────────────┘
                  │                                 │
                  ▼                                 ▼
┌──────────────────────────┐       ┌──────────────────────────────┐
│   Google Gemini API      │       │   External Bot APIs          │
│                          │       │                              │
│  Model: gemini-2.0-      │       │  e.g. BrewMind Bot           │
│         flash-lite       │       │  Any OpenAI-compatible API   │
│                          │       │  Custom chatbot endpoints    │
│  Used for:               │       │                              │
│  • Synthetic user msgs   │       │  Auth: Basic / API Key /     │
│  • Built-in bot replies  │       │        Bearer Token          │
│  • Evaluation grading    │       │                              │
│  Key: GOOGLE_API_KEY     │       │  Format: { messages: [...] } │
└──────────────────────────┘       └──────────────────────────────┘
```

## User Flow

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  Step 1  │───▶│    Step 2     │───▶│    Step 3     │───▶│   Run Test   │
│ Choose   │    │ System Prompt │    │ Choose a      │    │              │
│ a Bot    │    │ (editable)    │    │ Persona       │    │ 🚀 Start     │
│ 🛠️       │    │ OR            │    │ 🎭            │    │              │
│          │    │ External API  │    │              │    │              │
│          │    │ Config (URL + │    │              │    │              │
│          │    │ Auth)         │    │              │    │              │
└──────────┘    └──────────────┘    └──────────────┘    └──────┬───────┘
                                                               │
                                                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        SIMULATION LOOP                                   │
│                                                                         │
│   ┌──────────┐  persona msg   ┌───────────┐  bot reply  ┌───────────┐ │
│   │ Persona  │───────────────▶│ Target Bot│────────────▶│ Transcript│ │
│   │ Agent    │◀───────────────│           │             │ Builder   │ │
│   │ (Gemini) │   feed reply   │ (Gemini / │             │           │ │
│   │          │   as context   │  External)│             │           │ │
│   └──────────┘                └───────────┘             └─────┬─────┘ │
│                                                               │       │
│   Repeat for 2-8 turns (3s pause between each)                │       │
│                                                               │       │
└───────────────────────────────────────────────────────────────┼───────┘
                                                                │
                                                                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        EVALUATION PHASE                                  │
│                                                                         │
│   Full transcript + Persona context ──▶ Gemini (Evaluation Prompt)      │
│                                                                         │
│   Output:                                                               │
│   ┌──────────┬──────────┬───────────┬──────────┬──────────────┐        │
│   │ Clarity  │Helpful-  │Tone &     │ Safety & │Adaptability  │        │
│   │ /10      │ness /10  │Empathy /10│Bounds /10│ /10          │        │
│   └──────────┴──────────┴───────────┴──────────┴──────────────┘        │
│   + Overall Score + Strengths + Failures + Recommendation               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## Tech Stack

```
┌─────────────────────────────────────────────────────────┐
│                    FRONTEND                              │
│  React 18  ·  Vite  ·  Inline CSS  ·  Single Page App  │
│  Fonts: Space Grotesk + JetBrains Mono                  │
│  Theme: Dark mode (#0C0C0F)                             │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│                    BACKEND                               │
│  Vercel Serverless Functions (Node.js)                  │
│  Single endpoint: /api/chat                             │
│  Secure API key storage (env vars)                      │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│                    AI SERVICES                           │
│  Google Gemini 2.0 Flash Lite (personas + evaluation)   │
│  External Bot APIs (user-provided, proxied)             │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│                    DEPLOYMENT                            │
│  Vercel (Hobby plan)  ·  GitHub (source)                │
│  Auto-deploy on push  ·  HTTPS by default               │
└─────────────────────────────────────────────────────────┘
```

## File Structure

```
synth-evaluation/
├── api/
│   └── chat.js              # Vercel serverless proxy (Gemini + External APIs)
├── src/
│   ├── App.jsx              # Main app — 3-layer architecture (~600 lines)
│   └── main.jsx             # React entry point
├── index.html               # Vite HTML shell
├── package.json             # Dependencies (React 18, Vite)
├── vite.config.js           # Build config + dev proxy
├── vercel.json              # API routing rules
├── .env.example             # Environment variable template
└── README.md                # Documentation
```

## Security Model

```
  Browser (User)                  Vercel Serverless             External Services
  ──────────────                  ─────────────────             ─────────────────

  ❌ No API keys                  ✅ GOOGLE_API_KEY             🔒 Gemini API
  ❌ No direct API calls            stored securely               (authenticated)
  ❌ No external bot creds        ✅ Basic Auth added
                                    server-side                 🔒 External Bots
  ✅ Sends messages only          ✅ CORS bypass via               (authenticated
     to /api/chat                    server proxy                   via proxy)
```
